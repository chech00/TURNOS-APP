{
  "name": "turnos-app",
  "version": "1.0.0",
  "description": "Sistema de gestión de turnos NOC",
  "scripts": {
    "minify": "node scripts/minify.js",
    "build": "node scripts/minify.js && node scripts/use-minified.js --prod",
    "dev": "node scripts/use-minified.js --dev",
    "prod": "node scripts/use-minified.js --prod"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.36.0"
  }
}
